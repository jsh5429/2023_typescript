import React from 'react'

// Fetch를 통해서 외부 API(서버와 연결해서 데이터가져오는)를 사용하여
// 그 값을 가져와서 state에 저장해서 사용할 수 있다.
export default function FetchComp() {
    
  return (
    <div>
        <h3>FetchComp</h3>
    </div>
  )
}
